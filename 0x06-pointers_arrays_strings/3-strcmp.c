#include "main.h"

/**
 * _strcmp - compares two strings/
 * @s1: source.
 * @s2: destination.
 * Return: zero, negative or positive number if s1 is respectively,
 * equal, less or greater than s2.
 **/

int _strcmp(char *s1, char *s2)
{
	int count, x;

	for (
